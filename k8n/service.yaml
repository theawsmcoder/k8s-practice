apiVersion: v1 
kind: Service 
metadata:
  name: k8n-app-svc
  labels:
    app: test-app-svc
  namespace: k8n-ns
spec:
  type: NodePort
  selector:
    app: test-app
  ports:
    - port: 80
      nodePort: 30080
      targetPort: 8080
    
  
